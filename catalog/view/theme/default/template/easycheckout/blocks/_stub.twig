{# Generic stub для блоків без власного рендеру (Stage 4.1). #}
<div class="okec-block__inner okec-block__inner--stub">
  <div class="okec-block__header">
    <strong>{{ block.id }}</strong>
    <span class="okec-block__type-badge">{{ block.type }}</span>
  </div>
  <p class="okec-block__placeholder">Block "<code>{{ block.type }}</code>" — implementation coming in next iteration.</p>
</div>
